- table:
  - rowgroup:
    - row "Param Default Required Description":
      - columnheader "Param"
      - columnheader "Default"
      - columnheader "Required"
      - columnheader "Description"
  - rowgroup:
    - 'row "calcCellSize (w: number, h: number) => number (w, h) => Math.min(w / 7, h / 5) no Returns the size of each day''s square, given the chart width and height. The default fits seven columns across and five rows down."':
      - 'cell "calcCellSize (w: number, h: number) => number"':
        - strong: calcCellSize
        - code: "(w: number, h: number) => number"
      - cell "(w, h) => Math.min(w / 7, h / 5)":
        - code: (w, h) => Math.min(w / 7, h / 5)
      - cell "no"
      - cell "Returns the size of each day's square, given the chart width and height. The default fits seven columns across and five rows down."